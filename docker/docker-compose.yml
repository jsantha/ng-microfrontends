services:
  shell:
    build: ../apps/shell
    ports:
      - "5000:80"
    volumes:
      - type: bind
        source: ./modules.json
        target: /usr/share/nginx/html/assets/modules.json
  login:
    build: ../apps/login
    ports:
      - "5001:80"
